{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Azure PR Review settings",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "models",
    "steps"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "const": 2
    },
    "models": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "review"
      ],
      "properties": {
        "_help": {
          "type": "object",
          "additionalProperties": false,
          "description": "Documentation only; never forwarded as model instructions.",
          "properties": {
            "functional": {
              "type": "string"
            },
            "risk": {
              "type": "string"
            },
            "verifier": {
              "type": "string"
            }
          }
        },
        "review": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "functional",
            "risk",
            "verifier"
          ],
          "properties": {
            "functional": {
              "type": "string",
              "description": "Exact provider/model ID for independent correctness review. Select for code comprehension, project language, boundary behavior, and regressions."
            },
            "risk": {
              "type": "string",
              "description": "Exact provider/model ID for independent risk review, source checks, and comments. Select for cross-path failure analysis and reliable MCP tool use. Standalone /pr-check uses review.risk."
            },
            "verifier": {
              "type": "string",
              "description": "Exact provider/model ID for source-based verification, counterevidence, duplicate merging, and the final report. Select for evidence judgment, long context, and instruction following, not just summarization."
            }
          }
        },
        "deep": {
          "type": "object",
          "additionalProperties": false,
          "description": "Optional deep profile. Missing or empty roles disable deep mode; it never falls back to review models.",
          "properties": {
            "functional": {
              "type": "string",
              "description": "Exact provider/model ID for independent correctness review. Select for code comprehension, project language, boundary behavior, and regressions. May be empty; deep runs only when all three IDs are configured."
            },
            "risk": {
              "type": "string",
              "description": "Exact provider/model ID for independent risk review, source checks, and comments. Select for cross-path failure analysis and reliable MCP tool use. Standalone /pr-check uses review.risk. May be empty; deep runs only when all three IDs are configured."
            },
            "verifier": {
              "type": "string",
              "description": "Exact provider/model ID for source-based verification, counterevidence, duplicate merging, and the final report. Select for evidence judgment, long context, and instruction following, not just summarization. May be empty; deep runs only when all three IDs are configured."
            }
          }
        }
      }
    },
    "azure": {
      "deprecated": true,
      "description": "Obsolete, ignored. Retained only so existing installed profiles can be reused. Configure MCP and permissions in OpenCode."
    },
    "steps": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "check",
        "initial",
        "deep",
        "final"
      ],
      "properties": {
        "check": {
          "type": "integer",
          "minimum": 1,
          "maximum": 500
        },
        "initial": {
          "type": "integer",
          "minimum": 1,
          "maximum": 500,
          "description": "Iteration budget for each of the two normal-mode initial reviewers."
        },
        "deep": {
          "type": "integer",
          "minimum": 1,
          "maximum": 500,
          "description": "Iteration budget for each of the two deep-mode initial reviewers."
        },
        "final": {
          "type": "integer",
          "minimum": 1,
          "maximum": 500
        }
      }
    },
    "comments": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "enabled",
        "maxComments"
      ],
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Allow the explicit saved-preview publishing stage. MCP permissions remain governed by OpenCode."
        },
        "maxComments": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10,
          "default": 5,
          "description": "Maximum comments in a saved preview; publication follows prompt instructions."
        }
      }
    },
    "auxiliaryModels": {
      "const": "preserve",
      "description": "Never change global auxiliary models."
    },
    "enabled": {
      "type": "boolean",
      "default": true
    },
    "outputLanguage": {
      "type": "string",
      "pattern": "^[A-Za-z]{2,3}(-[A-Za-z0-9]{2,8})*$",
      "maxLength": 63,
      "default": "en",
      "examples": [
        "en",
        "zh-TW",
        "zh-CN",
        "ja",
        "zh-Hant-TW"
      ],
      "description": "Shared output language for final report Markdown and Azure comment prose. Use a language tag; intermediate reviews, JSON fields, identifiers, severity labels, and status receipts are unchanged. Restart OpenCode after changes."
    },
    "returnReport": {
      "enum": [
        "receipt",
        "full"
      ],
      "default": "receipt"
    },
    "structuredOutput": {
      "type": "boolean",
      "default": true,
      "description": "Use the OpenCode 1.18.31 StructuredOutput transport for review envelopes. Set false only for a provider that cannot use it; no automatic retry or fallback model."
    },
    "debug": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "enabled"
      ],
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Save private stage inputs, visible answers, errors, and results locally. May contain confidential source or model-echoed secrets. No automatic cleanup."
        },
        "directory": {
          "type": "string",
          "default": "",
          "description": "Empty uses XDG_STATE_HOME/opencode/azpr-debug (default ~/.local/state/opencode/azpr-debug). Otherwise an absolute path or path relative to the active OpenCode project directory. No tilde expansion."
        }
      }
    },
    "runTimeoutSeconds": {
      "type": "integer",
      "minimum": 10,
      "maximum": 7200,
      "default": 1200
    },
    "maxStageCharacters": {
      "type": "integer",
      "minimum": 1000,
      "maximum": 1000000,
      "default": 250000
    }
  }
}
