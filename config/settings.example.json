{
  "$schema": "./settings.schema.json",
  "version": 2,
  "models": {
    "_help": {
      "functional": "Independent correctness review: requirements, boundaries, API compatibility, and regressions. Prefer strong code comprehension in your project language. All roles need reliable MCP tools, structured output, approved data handling, and acceptable cost.",
      "risk": "Independent risk review: failures, retries, concurrency, authorization, and data consistency. Prefer evidence-based cross-path reasoning and reliable tool use. Also handles source checks and comments for this mode; standalone /pr-check uses review.risk.",
      "verifier": "Verify both initial reviews against source, find counterevidence, merge duplicates, and produce the final report. Prefer strong evidence judgment, long-context handling, and instruction following. Never just summarize or vote. Roles may reuse the same model ID."
    },
    "review": {
      "functional": "REPLACE_PROVIDER/REPLACE_FUNCTIONAL",
      "risk": "REPLACE_PROVIDER/REPLACE_RISK",
      "verifier": "REPLACE_PROVIDER/REPLACE_VERIFIER"
    },
    "deep": {
      "functional": "",
      "risk": "",
      "verifier": ""
    }
  },
  "steps": {
    "check": 24,
    "initial": 60,
    "deep": 80,
    "final": 100
  },
  "comments": {
    "enabled": false,
    "maxComments": 5
  },
  "auxiliaryModels": "preserve",
  "enabled": true,
  "returnReport": "receipt",
  "structuredOutput": true,
  "debug": {
    "enabled": false,
    "directory": ""
  },
  "outputLanguage": "en",
  "runTimeoutSeconds": 1200,
  "maxStageCharacters": 250000
}
