{
  "name": "opencode-azure-devops-pr-review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Opt-in multi-model Azure DevOps pull request reviews for OpenCode",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/runtime.mjs && node --check src/comments.mjs && node --check src/plugin.js && sh -n install.sh && sh -n uninstall.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/WTLAIB/opencode-azure-devops-pr-review.git"
  }
}
